#!/usr/bin/env ruby

require 'rubygems'
require File.join(File.dirname(__FILE__), '..', 'lib', 'username_check')

REDCODE   = 31
GREENCODE = 32

UsernameCheck.check_all(ARGV.first).each do |service, used|
  code = used ? REDCODE : GREENCODE
  puts "\e[#{ code }m#{service}\e[0m"
end